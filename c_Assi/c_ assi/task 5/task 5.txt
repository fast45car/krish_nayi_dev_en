1.Create a simple IPL Fan Bot that takes your favorite IPL team name as input and uses if-else-if statements to print a unique cheer message for each team (e.g., 'Go Mumbai Indians!', 'Chennai Super Kings for the win!'). If the team is not recognized, print 'Team not found!'

#include<stdio.h>
#include<conio.h>
#include<string.h>
int main(){
  char team[50];
  
  printf("enter The IPL Favourite team : ");
  scanf("%s",&team);
  
  if(strcmp(team,"Mumbai Indians")==0){
  	printf("go Mumbai Indians !");
  }
  else if(strcmp(team,"rcb")==0){
  	printf("i Sala Kapnambu rcb");
  }
  
   else if(strcmp(team,"Gt")==0){
  	printf("ava de Gt");
  }
  
   else if(strcmp(team,"csk")==0){
  	printf("thala for a reson csk");
  }
  
   else if(strcmp(team,"pnb")==0){
  	printf("oy panjabi agayy Punjab king ");
  	
  
  }
  else{
  	printf("the team is not found");
  }
  return 0;
}


2.Build a Zomato-style food suggestion tool: take the user's preferred meal time ('breakfast', 'lunch', 'dinner', or 'snack') and use a switch-case statement to suggest a popular dish for that time. If the input doesn't match any meal, suggest 'Try some fruits!'.


#include<stdio.h>
#include<conio.h>
#include<string.h>

int main(){
char dish[100];

printf("!welcome to zometo! enter the food Category (breakfast/lunch/dinner) : ");
scanf("%s",&dish);

switch(dish[0]){
	case 'b':
		if(strcmp(dish,"breakfast")==0){
		printf(".dabeli");
		printf("\n.vadapaou");
		printf("\n.Dhokla");
		printf("\n.Thepla");
		printf("\n.Gathiya & Jalebi");
		printf("\n.Khaman with Papdi & Sev:");	
		}
	
	

	case 'l':
		if(strcmp(dish,"lunch")==0){
		printf(".Gujarati thali");
		printf("\n.pnajabi thali");
		printf("\n.Dry Vegetable Sabzis");
		printf("\n.Farsan");
		printf(".Idli-Sambhar");
		}
		

	case 'd':
		if(strcmp(dish,"dinner")==0){
	    printf("Dal Dhokli");
		printf("\nGujarati Dal, Bhaat, Rotli, Shaak");
		printf("\nSev Tameta nu Shaak with Rotli");
		printf("\n dal bati");
		printf("Kadhi with Khichdi variation (Masala Khichdi)");
		}
		
	
	
		

default :
printf("Try some fruite the dish is not avilabel");	
}

return 0;
}


3.Create a Flipkart discount calculator that asks the user for the total cart amount. Use nested if statements to check: if amount > 2000, apply 20% discount; else if amount > 1000, apply 10% discount; else, no discount. Print the final amount to pay.<br><br><em><strong>Hint:</strong> Use nested ifs to check each discount slab.</em>

#include<stdio.h>
#include<conio.h>


int main(){
	int amount;
	int price;
	int sum;
	int total;
	int seconsum,seconamount,secontotal;
	
	
	printf("enter the filpcart price rate : ");
	scanf("%d",&amount);
	
	sum = amount * 20 / 100;
	total = amount - sum;
	
		sum = amount * 10 / 100;
	total =  amount - sum;  
	
	if(amount >= 2000){
		printf("you get 20%% Discount");
		printf("\nthe Discount : %d",sum);
		printf("\nthe final bill is : %d",total);
	}
  else if(amount >= 1000){
  	printf("you get 10%% Discount");
		printf("\nthe Discount : %d",sum);
		printf("\nthe final bill is : %d",total);
  }
  else{
  	printf("the Discount is not avilabe %d",amount);
  }
  return 0;
}


4.Write a program that takes a user's age and checks eligibility for three things using if-else statements: if age >= 18, print 'Eligible for Driving License'; if age >= 21, also print 'Eligible for Credit Card'; if age >= 25, also print 'Eligible for Car Rental'. Print all applicable messages for the given age.
Topic Repository URL


#include<stdio.h>
#include<conio.h>

int main(){
	int age;
	int card;
	int car;
	printf("enter the age : ");
	scanf("%d",&age);
	if(age >= 18 && age < 21){
		printf("Eligible for Driving License");
	}
	else if(age >= 21 && age < 25){
		printf("Eligible for Credit Card");
	}
	else if(age >= 25){
		printf("Eligible for Car Rental");
	}
	else{
		printf("the programe is invalid");
	}
	return 0;
}